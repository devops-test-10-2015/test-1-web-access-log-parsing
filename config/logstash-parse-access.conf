input {
    file {
        path => "/tmp/access.log"
        start_position => beginning 
    }
}
filter {
    grok {
          match => { "message" => "%{COMMONAPACHELOG} %{BASE10NUM:time_to_serve}"}
    }
}
output {
  #stdout { codec => json }
  stdout { codec => rubydebug }
}

